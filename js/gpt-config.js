/** GPT API 설정 — API 키는 챗봇 UI에서 입력 (localStorage 저장) */
window.GPT_CONFIG = {
  model: 'gpt-4o-mini',
  endpoint: 'https://api.openai.com/v1/chat/completions',
  maxTokens: 800,
  temperature: 0.7,
};
